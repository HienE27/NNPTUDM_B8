# Mailtrap (Email Testing) — copy file này thành .env và điền giá trị
# Lấy Username / Password tại: Mailtrap → Sandbox → Integration → Credentials

MAIL_HOST=sandbox.smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USER=
MAIL_PASS=
